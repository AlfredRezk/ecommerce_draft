# 01. install ai tools and vscode extensions

1. install codeium extension
2. create codeium account
3. use codeium in vscode
4. install vscode extensions
   1. eslint
   2. prettier
   3. vscode-icons
   4. tailwindcss
   5. es7+ react snippets
   6. pretty typscript errors

# 02. install shadcn npx shadcn@latest init

npm i embla-carousel-autoplay
npm i react-medium-image-zoom

# Auth

1. npx shadcn@latest add form dropdown-menu
2. npm i next-auth@beta bcryptjs react-hook-form @hookform/resolvers mongodb @auth/mongodb-adapter
3. npm i @types/bcryptjs

# Google Auth

- open https://console.cloud.google.com/apis/credentials
- create a new OAuth 2.0 Client ID and get its credentials
  AUTH_GOOGLE_ID=
  AUTH_GOOGLE_SECRET=

# create checkout page

## install packages

npx shadcn@latest add radio-group

# pay-order-by-paypal

## install packages

npm i resend @react-email/components @paypal/react-paypal-js --legacy-peer-deps
npm i -D react-email --legacy-peer-deps
npx shadcn@latest add badfe table

### Resend get api keys from https://resend.com/api-keys

# stripe

pass=AlfredAlbert@2025
npm i stripe @stripe/stripe-js @stripe/react-stripe-js --legacy-peer-deps
https://dashboard.stripe.com/test/apikeys
